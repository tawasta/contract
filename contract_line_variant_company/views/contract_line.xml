<?xml version="1.0"?>
<odoo>

    <record id="view_contract_line:variant_company_tree" model="ir.ui.view">
        <field name="name">contract.line.variant.company.tree</field>
        <field name="model">contract.line</field>
        <field name="inherit_id" ref="contract.contract_line_tree_view"/>
        <field name="arch" type="xml">
            <field name="product_id" position="after">
                <field name="variant_company_id"/>
            </field>
        </field>
    </record>

</odoo>
